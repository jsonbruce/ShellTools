#! /usr/bin/expect
# Copy my ssh pub key to other node

set ip [lindex $argv 0]
set timeout 60
set password "111111"

spawn ssh-copy-id root@$ip
expect {
	"(yes/no)?" {
		send "yes\r"
		exp_continue 
	}
	"password:" {
		send "$password\r"
	}
}
interact	
